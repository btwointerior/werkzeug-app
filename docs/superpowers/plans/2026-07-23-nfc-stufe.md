# NFC-Stufe Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** NFC-Lesen (alle) + NFC-Schreiben mit Pflicht-Versiegelung (Admin) in der iOS-App gemäß Spec `2026-07-23-nfc-stufe-design.md`.

**Architecture:** Lokales Capacitor-Plugin `WerkzeugNfc` (Swift/Core NFC) + dünne JS-Schicht `frontend/js/nfc.js` hinter der bestehenden `holeWerkzeugCode`-Abstraktion. Apple-Capability + neues Profil; Codemagic-Re-Fetch durch User.

**Tech Stack:** Core NFC (NFCNDEFReaderSession, writeNDEF, writeLock), Capacitor 7, Vanilla JS.

## Global Constraints

- Branch `nfc` von `main`; Tests: `node --test tests/js/*.test.mjs` + pytest (venv ~/.venvs/werkzeug); vboxsf-npm nur `--no-bin-links`
- KEIN window.confirm/alert (iOS-PWA-Lehre) — DOM-Dialoge
- NFC-Buttons nur bei verfügbarem Plugin sichtbar; Schreiben nur Rolle ADMIN; Versiegeln PFLICHT
- Bundle-ID `de.b2interior.werkzeug` (ASC-ID AGNH5G7949), App 6793565529, Cert 3GQ7JNMNZS, ASC-Key `/media/sf_claude_project/Apple_Keys/bau.os Key/` (JWT-Helper /tmp/asc_token.py), Codemagic-App `6a60da38c7e1be082d72a1cf`

---

### Task N1: JS-Schicht (nfc.js, Overlay-Button, Admin-Schreib-Button) — TDD für pure Helfer

**Files:** Create `frontend/js/nfc.js`, Test `tests/js/nfc.test.mjs`, Modify `frontend/js/scanner.js` (Overlay-Button), `frontend/js/views/maschine.js` (Admin-Button)

- `nfc.js`: `nfcPlugin()` (window.Capacitor?.registerPlugin('WerkzeugNfc'), einmalig gecacht), `istNfcVerfuegbar()`, `nfcLeseCode()` (readCode → parseScan), `baueTagUrl(base, code)` (pure, getestet), `nfcSchreibeUndVersiegele(code)`
- Overlay: Button „NFC-Tag lesen" unten im QR-Overlay (nur wenn verfügbar) → resolved den Overlay-Promise mit NFC-Code
- Maschinen-Seite: Admin+NFC → Button „NFC-Tag beschreiben"; DOM-Bestätigung („dauerhaft versiegelt!") → schreiben → Erfolgs-/Fehler-Toast
- [ ] Tests rot → implementieren → grün → alle Suiten → Commit

### Task N2: Swift-Plugin + Projektverdrahtung

**Files:** Create `ios_app/ios/App/App/WerkzeugNfcPlugin.swift`, `ios_app/ios/App/App/MyViewController.swift`, `ios_app/ios/App/App/App.entitlements`; Modify `Main.storyboard` (customClass), `project.pbxproj` (CODE_SIGN_ENTITLEMENTS), `Info.plist` (NFCReaderUsageDescription)

- Plugin: `readCode` (Session, invalidateAfterFirstRead, URI/Text-Payload → {text}), `writeAndLock` (didDetect tags → connect → status prüfen → writeNDEF(URI) → writeLock → resolve; readOnly → Fehler)
- deutsche alertMessages; Fehler → reject mit Meldung
- [ ] Dateien anlegen, pbxproj/storyboard/Info.plist patchen, Commit (Kompiliert-Beweis erst via Codemagic in N4)

### Task N3: Apple-Capability + neues Profil

- [ ] POST /v1/bundleIdCapabilities {NFC_TAG_READING, bundleId AGNH5G7949}
- [ ] Altes Profil „Werkzeug App Store" löschen (Capability-Änderung invalidiert), neues „Werkzeug App Store NFC" mit Cert 3GQ7JNMNZS anlegen
- [ ] **User-Klick:** Codemagic → Code signing identities → Fetch profiles (neues Profil übernehmen) — danach erst N4

### Task N4: Build + Abnahme

- [ ] Merge nfc→main, Tag `app-v1.1.0`, Push; Build per API auf Codemagic-App `6a60da38c7e1be082d72a1cf`; Apple-Processing VALID abwarten
- [ ] Geräte-Abnahme User (sobald Tags da): Lesen alle / Schreiben+Versiegeln Admin / Versiegelung mit NFC-Tools gegenprüfen; Memory aktualisieren
